# Track the publishable Unity player so every rebuild appears in GitHub Desktop.
# Keep debugging symbols and Unity's explicitly non-shipping output private.
*_BurstDebugInformation_DoNotShip/
*.pdb
*.mdb
*.debug
*.dmp
*.log
Crashes/
crashes/
